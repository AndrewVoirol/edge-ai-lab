{
  "name": "macOS Navigation Efficiency",
  "description": "Sidebar navigation on macOS: cycle through all major sections (Benchmarks → Evaluations → Conversations → Models) and verify each section's detail column loads. Validates NavigationSplitView routing and that no section crashes on selection.",
  "platform": "macOS",
  "steps": [
    {
      "step": 1,
      "action": "verify_ui",
      "description": "Verify sidebar and detail column are available.",
      "expected_elements": ["sidebar_list", "detailColumn_root"]
    },
    {
      "step": 2,
      "action": "tap",
      "description": "Navigate to the Benchmarks Dashboard via sidebar.",
      "target_element": "sidebar_benchmarks_dashboard"
    },
    {
      "step": 3,
      "action": "wait",
      "description": "Wait for dashboard to render.",
      "timeout_seconds": 2
    },
    {
      "step": 4,
      "action": "tap",
      "description": "Navigate to the Evaluations section via sidebar.",
      "target_element": "sidebar_evaluations_run"
    },
    {
      "step": 5,
      "action": "wait",
      "description": "Wait for eval runner view to appear.",
      "condition": "element_exists:evalRunnerView_root",
      "timeout_seconds": 5
    },
    {
      "step": 6,
      "action": "verify_ui",
      "description": "Verify eval runner loaded.",
      "expected_elements": ["evalRunnerView_root"]
    },
    {
      "step": 7,
      "action": "tap",
      "description": "Navigate to the Conversations section via sidebar.",
      "target_element": "sidebar_newChat"
    },
    {
      "step": 8,
      "action": "verify_ui",
      "description": "Verify chat column is still responsive after navigation.",
      "expected_elements": ["chatColumn_root"]
    }
  ]
}
