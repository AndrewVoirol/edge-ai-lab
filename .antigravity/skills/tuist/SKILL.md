---
name: tuist
description: "Project generation and management using Tuist. Activate when editing Project.swift, adding targets, changing dependencies, or modifying build settings."
---

# Tuist Project Management

## Core Principle
`Project.swift` is the **single source of truth** for the Xcode project. All generated artifacts (`.xcodeproj`, `.xcworkspace`, `Derived/`) are ephemeral and regenerated by `tuist generate`.

## Critical Rules

> [!CAUTION]
> **NEVER** directly edit `.xcodeproj`, `.xcworkspace`, or anything in `Derived/`. These are Tuist-generated and will be overwritten.

- ALL project changes go through `Project.swift`
- After ANY edit to `Project.swift`, run `tuist generate`
- New Swift files added to `Sources/` or `Tests/` require `tuist generate` to be picked up
- The `.xcworkspace` file has a `.tuist-generated` marker — respect it

## Project Structure

| Target | Platform | Bundle ID | Deployment |
|---|---|---|---|
| `GemmaEdgeGallery_iOS` | iOS | `com.andrewvoirol.GemmaEdgeGallery` | iOS 26.5+ |
| `GemmaEdgeGallery_iOSTests` | iOS | `com.andrewvoirol.GemmaEdgeGallery.Tests` | iOS 26.5+ |
| `GemmaEdgeGallery_macOS` | macOS | `com.andrewvoirol.GemmaEdgeGallery.mac` | macOS 26.0+ |
| `GemmaEdgeGallery_macOSTests` | macOS | `com.andrewvoirol.GemmaEdgeGallery.mac.Tests` | macOS 26.0+ |

## Code Signing
- **Team ID:** `ASX83B274M` (Free Personal Team)
- **Style:** `Automatic`
- Set via `DEVELOPMENT_TEAM` environment variable or defaults to `ASX83B274M`
- **Do NOT remove** the Team ID from `Project.swift`
- Free teams cannot use `fastlane match` or `sigh` — provisioning is local-only

## Dependencies
- `LiteRT-LM` from `https://github.com/google-ai-edge/LiteRT-LM.git` (branch: `main`)

## Workflow
1. Edit `Project.swift` (targets, deps, settings, Info.plist)
2. Run `tuist generate` (or use the script: `.antigravity/skills/tuist/scripts/generate.sh`)
3. Build using XcodeBuildMCP tools

## What Goes in Project.swift
- Target definitions (name, platform, product type)
- Bundle identifiers
- Deployment targets
- Info.plist extensions
- Dependencies (packages, targets)
- Build settings (code signing, team ID)
- Source/test file globs
